fn main() {
    let _x = 2.0; // f64

    let _y: f32 = 3.0; // f32\

    // Unsigned numbers
    // will return error in debug mode
    // loop back to end in release mode

    // Numeric operations
    // addition
    let _sum = 5 + 10;

    // subtraction
    let _difference = 95.5 - 4.3;

    // multiplcation

    // Compound Types
    // Tuples

    // fixed length, cannot grow or shrink in size
    let _tup: (i32, f64, u8) = (500, 6.4, 1);

    // pull values with tup.0, tup.1, etc.

    // tuple w/o any values is called a unit

    // Array Type
    // Unlike types, every value must have the same type
    // Also can't shrink or grow - vectors can though
    // use brackets to acces x[0], x[1]

}
